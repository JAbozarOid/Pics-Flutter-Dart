// 1- for working with flutter widgets we should import material library
import 'package:flutter/material.dart';
import 'src/app.dart';

// 2- define a main fuction
// first of all, define a 'main' function to run when our app starts
void main() {
  // * there is two separate things
  // * first create a new text widget to show some text on the screen
  // * second take that widget and get it on the screen
  runApp(App());
}
